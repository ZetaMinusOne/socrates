---
phase: 06-plugin-scaffold-and-path-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - socrates/skills/socrates/SKILL.md
autonomous: false
requirements:
  - PATH-01
  - PATH-02
  - PATH-03

must_haves:
  truths:
    - "All 24 .claude/skills/socrates/ path references in SKILL.md are replaced with the verified plugin-relative prefix"
    - "Preflight check reads the correct plugin-relative path for dialectics.opt.cue"
    - "User invokes /socrates after plugin install and protocol file reads succeed without file-not-found errors"
    - "The --record flag reads recording.cue via the correct plugin-relative path"
    - "Preflight error message is appropriate for plugin installs (no submodule-specific language)"
  artifacts:
    - path: "socrates/skills/socrates/SKILL.md"
      provides: "Fully migrated path references for plugin distribution"
      min_lines: 300
  key_links:
    - from: "socrates/skills/socrates/SKILL.md"
      to: "socrates/protocols/dialectics.opt.cue"
      via: "Preflight check Read instruction"
      pattern: "protocols/dialectics.opt.cue"
    - from: "socrates/skills/socrates/SKILL.md"
      to: "socrates/protocols/routing.opt.cue"
      via: "Routing section Read instruction"
      pattern: "protocols/routing.opt.cue"
    - from: "socrates/skills/socrates/SKILL.md"
      to: "socrates/dialectics/governance/recording.cue"
      via: "--record flag conditional Read instruction"
      pattern: "dialectics/governance/recording.cue"
---

<objective>
Migrate all 24 hardcoded `.claude/skills/socrates/` path references in SKILL.md to use the plugin-relative path prefix determined by Plan 1's empirical testing. Update the preflight error message for plugin installs.

Purpose: After this plan, users who install via `--plugin-dir ./socrates` can invoke `/socrates <problem>` and all protocol file reads resolve correctly. This is the core PATH-01/PATH-02/PATH-03 deliverable.

Output: SKILL.md with all paths migrated and verified end-to-end.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-RESEARCH.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all path references and update preflight message</name>
  <files>socrates/skills/socrates/SKILL.md</files>
  <action>
**CRITICAL: Read 06-01-SUMMARY.md first** to determine the path prefix strategy. The summary from Plan 1 documents whether `$CLAUDE_PLUGIN_ROOT` expands in SKILL.md Read paths and what prefix to use.

**Path migration (24 occurrences):**

Replace ALL occurrences of `.claude/skills/socrates/` in SKILL.md with the verified prefix from Plan 1's findings. The 24 occurrences break down as:

| Location | Count | Current Path Pattern | Target |
|----------|-------|---------------------|--------|
| Preflight (lines ~11, ~16) | 2 | `.claude/skills/socrates/protocols/dialectics.opt.cue` | `{prefix}protocols/dialectics.opt.cue` |
| recording.cue (lines ~38, ~302) | 2 | `.claude/skills/socrates/dialectics/governance/recording.cue` | `{prefix}dialectics/governance/recording.cue` |
| Protocol Files header (line ~42) | 1 | `.claude/skills/socrates/protocols/` | `{prefix}protocols/` |
| Kernel (lines ~45-46) | 2 | `.claude/skills/socrates/protocols/dialectics.opt.cue` and `routing.opt.cue` | `{prefix}protocols/...` |
| Adversarial list (lines ~49-54) | 6 | `.claude/skills/socrates/protocols/adversarial/{x}.opt.cue` | `{prefix}protocols/adversarial/{x}.opt.cue` |
| Evaluative list (lines ~57-62) | 6 | `.claude/skills/socrates/protocols/evaluative/{x}.opt.cue` | `{prefix}protocols/evaluative/{x}.opt.cue` |
| Exploratory (line ~65) | 1 | `.claude/skills/socrates/protocols/exploratory/adp.opt.cue` | `{prefix}protocols/exploratory/adp.opt.cue` |
| Routing read (line ~69) | 1 | `.claude/skills/socrates/protocols/routing.opt.cue` | `{prefix}protocols/routing.opt.cue` |
| Adversarial execution (line ~180) | 1 | `.claude/skills/socrates/protocols/adversarial/{acronym}.opt.cue` | `{prefix}protocols/adversarial/{acronym}.opt.cue` |
| Evaluative execution (line ~221) | 1 | `.claude/skills/socrates/protocols/evaluative/{acronym}.opt.cue` | `{prefix}protocols/evaluative/{acronym}.opt.cue` |
| ADP execution (line ~237) | 1 | `.claude/skills/socrates/protocols/exploratory/adp.opt.cue` | `{prefix}protocols/exploratory/adp.opt.cue` |

Use a deterministic find-and-replace: replace ALL occurrences of `.claude/skills/socrates/` with `{prefix}`. This catches all 24. Verify the count after replacement.

**If `$CLAUDE_PLUGIN_ROOT` works (from Plan 1 findings):**
- Prefix = `$CLAUDE_PLUGIN_ROOT/socrates/`
- All 24 replacements are: `.claude/skills/socrates/` → `$CLAUDE_PLUGIN_ROOT/socrates/`

**If `$CLAUDE_PLUGIN_ROOT` does NOT work (from Plan 1 findings):**
- Read 06-01-SUMMARY.md for the alternative strategy documented there
- The summary will specify the exact prefix or approach (e.g., relative paths, meta-instruction for Claude to resolve paths from SKILL.md location, etc.)
- Apply whatever strategy was validated in Plan 1

**Update preflight error message:**

Replace the current error message in the Preflight section. Change from:
```
"Setup required: the dialectics submodule is not initialized or protocol files have not been generated.
Run: git submodule update --init --recursive
Then check that .claude/skills/socrates/protocols/dialectics.opt.cue exists.
If missing, regenerate the optimized protocol files from the dialectics submodule."
```

To a message appropriate for plugin installs (per Claude's discretion from CONTEXT.md):
```
"Setup required: protocol files are missing or unreadable. This usually means the plugin was not installed correctly.
Try reinstalling the plugin, or check that the protocols/ directory exists within the plugin root."
```

The new message should NOT reference submodule commands or `.claude/skills/` paths since those are developer-facing, not plugin-user-facing.

**Preserve everything else:**
- All SKILL.md frontmatter unchanged (`name: socrates`, `disable-model-invocation: true`, `allowed-tools: Read`)
- All execution logic, routing, output rendering sections unchanged
- Only paths and the preflight error message are modified
  </action>
  <verify>
    <automated>grep -c '\.claude/skills/socrates/' socrates/skills/socrates/SKILL.md | grep -q '^0$' && echo "Zero old paths remain — PASS" || echo "FAIL: old paths still present"</automated>
  </verify>
  <done>
    - Zero occurrences of `.claude/skills/socrates/` remain in SKILL.md
    - All 24 path references use the verified plugin-relative prefix
    - Preflight error message updated for plugin installs
    - Preflight path points to correct location for dialectics.opt.cue
    - recording.cue path (used by --record) points to correct plugin-relative location
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification with --plugin-dir</name>
  <what-built>All 24 path references in SKILL.md have been migrated from `.claude/skills/socrates/` to the plugin-relative prefix. The preflight error message has been updated. This task verifies the complete flow works end-to-end.
  </what-built>
  <how-to-verify>
**Test 1: Plugin loads and preflight passes**
Run: `claude --plugin-dir ./socrates`
Invoke: `/socrates test problem`
Expected: The preflight check reads the protocol file successfully (no file-not-found error). Routing proceeds and selects a protocol.

**Test 2: Protocol file reads work**
Expected: After routing, the selected protocol's `.opt.cue` file is read successfully. The execution section runs without path errors.

**Test 3: Verify --record path (optional but valuable)**
Run: `/socrates --record test problem`
Expected: The `recording.cue` file is read successfully when `--record` flag is present.

**Test 4: Confirm zero old paths**
In the repo (outside Claude session):
Run: `grep -c '.claude/skills/socrates/' socrates/skills/socrates/SKILL.md`
Expected: 0

**Report back with:**
1. Did the preflight pass?
2. Did protocol execution work end-to-end?
3. Did --record path resolve?
4. Any errors or unexpected behavior?
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `grep -c '.claude/skills/socrates/' socrates/skills/socrates/SKILL.md` returns 0
- [ ] Preflight check path points to correct plugin-relative location
- [ ] recording.cue path (--record flag) points to correct plugin-relative location
- [ ] Preflight error message does not reference submodule commands or old paths
- [ ] `/socrates` invocation via `--plugin-dir` passes preflight and executes protocol (human-verified)
</verification>

<success_criteria>
All path references migrated, zero old paths remain, and end-to-end invocation via `--plugin-dir ./socrates` works: preflight passes, protocol files load, execution completes without file-not-found errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-plugin-scaffold-and-path-migration/06-02-SUMMARY.md`
</output>
