---
phase: 06-plugin-scaffold-and-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - socrates/.claude-plugin/plugin.json
  - socrates/skills/socrates/SKILL.md
  - .gitmodules
autonomous: false
requirements:
  - PLUG-03
  - PLUG-04

must_haves:
  truths:
    - "plugin.json exists at socrates/.claude-plugin/plugin.json with name, description, author, homepage, repository, and license fields"
    - "SKILL.md lives at socrates/skills/socrates/SKILL.md (moved from socrates/SKILL.md)"
    - ".gitmodules references socrates/dialectics (not .claude/skills/socrates/dialectics)"
    - "Running claude --plugin-dir ./socrates loads the plugin without manifest errors"
  artifacts:
    - path: "socrates/.claude-plugin/plugin.json"
      provides: "Plugin manifest with identity metadata"
      contains: "socrates-skill"
    - path: "socrates/skills/socrates/SKILL.md"
      provides: "Skill entrypoint at plugin-convention location"
      contains: "name: socrates"
    - path: ".gitmodules"
      provides: "Corrected submodule path reference"
      contains: "socrates/dialectics"
  key_links:
    - from: "socrates/.claude-plugin/plugin.json"
      to: "socrates/skills/socrates/SKILL.md"
      via: "Plugin autodiscovery convention"
      pattern: "skills/socrates/SKILL.md"
---

<objective>
Create the plugin manifest, restructure the directory to match plugin conventions, fix .gitmodules, and empirically test `--plugin-dir` to determine whether `$CLAUDE_PLUGIN_ROOT` expands in SKILL.md Read-tool paths.

Purpose: Establish the plugin scaffold so Phase 6 Plan 2 can migrate paths with a verified strategy. The empirical test is critical -- research identified bug #9354 (open since Oct 2025) showing `$CLAUDE_PLUGIN_ROOT` does NOT expand in skill markdown text. This plan determines the actual behavior before committing to a 24-reference migration.

Output: plugin.json manifest, SKILL.md at correct plugin location, fixed .gitmodules, and documented path resolution findings.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest and restructure directory</name>
  <files>socrates/.claude-plugin/plugin.json, socrates/skills/socrates/SKILL.md, .gitmodules</files>
  <action>
**Step 1: Create plugin.json manifest**

Create `socrates/.claude-plugin/plugin.json` with the following exact content (per locked decisions from CONTEXT.md):

```json
{
  "name": "socrates-skill",
  "description": "Structured dialectic reasoning via 13 protocols. Invoke explicitly with /socrates — never auto-applies to problems.",
  "author": {
    "name": "zetaminusone"
  },
  "homepage": "https://zetaminusone.com",
  "repository": "https://github.com/riverline-labs/socrates",
  "license": "MIT"
}
```

Note: `version` is intentionally omitted. Per STATE.md decision: "Version set only in marketplace.json for relative-path plugins: setting in plugin.json silently overrides." Version will be added in Phase 9 marketplace.json.

**Step 2: Move SKILL.md to plugin autodiscovery location**

Move `socrates/SKILL.md` to `socrates/skills/socrates/SKILL.md`. Create the `socrates/skills/socrates/` directory if needed. Use `git mv` to preserve history.

Preserve all existing SKILL.md content exactly. The `disable-model-invocation: true` and `allowed-tools: Read` frontmatter fields must remain unchanged (per locked decision: skill must only be invoked when explicitly called via `/socrates`).

**Step 3: Fix .gitmodules**

Update `.gitmodules` from:
```
[submodule ".claude/skills/socrates/dialectics"]
    path = .claude/skills/socrates/dialectics
    url = https://github.com/riverline-labs/dialectics.git
```

To:
```
[submodule "socrates/dialectics"]
    path = socrates/dialectics
    url = https://github.com/riverline-labs/dialectics.git
```

After updating .gitmodules, run `git submodule sync` to propagate the path change. This ensures `git submodule update --init` works for developers cloning the repo.
  </action>
  <verify>
    <automated>test -f socrates/.claude-plugin/plugin.json && python3 -c "import json; d=json.load(open('socrates/.claude-plugin/plugin.json')); assert d['name']=='socrates-skill'; assert 'version' not in d; assert d['author']['name']=='zetaminusone'; print('manifest OK')" && test -f socrates/skills/socrates/SKILL.md && ! test -f socrates/SKILL.md && grep -q 'socrates/dialectics' .gitmodules && ! grep -q '.claude/skills/socrates/dialectics' .gitmodules && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - plugin.json exists at socrates/.claude-plugin/plugin.json with name=socrates-skill, no version field, correct author/homepage/repository/license
    - SKILL.md moved to socrates/skills/socrates/SKILL.md (old location removed)
    - .gitmodules references socrates/dialectics with correct path and url
    - git submodule sync completed without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Test plugin loading and path resolution with --plugin-dir</name>
  <what-built>Plugin manifest and directory structure created. Now we need to empirically test two critical unknowns before path migration:

1. Does `--plugin-dir ./socrates` load the plugin and register the `/socrates` skill?
2. Does `$CLAUDE_PLUGIN_ROOT` expand when Claude uses the Read tool on a path from SKILL.md?

These questions cannot be answered without running Claude Code interactively. Research identified bug #9354 (CLAUDE_PLUGIN_ROOT doesn't expand in skill markdown) and bug #17271 (slash command invocation form inconsistency).
  </what-built>
  <how-to-verify>
**Test 1: Plugin loading**
Run: `claude --plugin-dir ./socrates`
Type: `/help` or press Tab after typing `/`
Expected: `/socrates` (or `/socrates-skill:socrates`) appears as an available command.

Document the EXACT invocation form that works (e.g., `/socrates`, `/socrates-skill:socrates`, or both).

**Test 2: Path resolution (current paths)**
In the same session, invoke the skill: `/socrates test problem`
Expected: The preflight check will try to read `.claude/skills/socrates/protocols/dialectics.opt.cue` (old path).
This WILL fail because the file is now at `socrates/protocols/dialectics.opt.cue`.
Confirm: the preflight fails with a file-not-found error. This proves path migration is needed.

**Test 3: Path resolution strategy**
Before closing the session, manually test whether `$CLAUDE_PLUGIN_ROOT` resolves:
Ask Claude: "Please read the file at path: `$CLAUDE_PLUGIN_ROOT/socrates/protocols/dialectics.opt.cue`"

Document the result:
- If Claude reads the file successfully → `$CLAUDE_PLUGIN_ROOT` expansion works. Plan 2 will use `$CLAUDE_PLUGIN_ROOT/socrates/` prefix.
- If Claude fails or treats it as a literal string → `$CLAUDE_PLUGIN_ROOT` does NOT expand. Plan 2 will need a different strategy (possibly relative paths from SKILL.md location, or a meta-instruction telling Claude to resolve paths relative to the plugin root).

**Report back with:**
1. Exact slash command invocation form that worked
2. Whether `$CLAUDE_PLUGIN_ROOT` expanded or not in the Read tool path
3. Any error messages or unexpected behavior
  </how-to-verify>
  <resume-signal>Report findings: (1) invocation form, (2) did $CLAUDE_PLUGIN_ROOT expand?, (3) any errors. Type "approved" with findings or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `socrates/.claude-plugin/plugin.json` exists and is valid JSON with required fields
- [ ] `socrates/skills/socrates/SKILL.md` exists with correct frontmatter
- [ ] `socrates/SKILL.md` no longer exists (moved, not copied)
- [ ] `.gitmodules` references `socrates/dialectics` path
- [ ] Plugin loads via `--plugin-dir ./socrates` (human-verified)
- [ ] Path resolution strategy documented for Plan 2 (human-verified)
</verification>

<success_criteria>
Plugin scaffold is in place (manifest + directory structure + fixed gitmodules) and the path resolution strategy is empirically determined so Plan 2 can execute the 24-reference migration with confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-plugin-scaffold-and-path-migration/06-01-SUMMARY.md`

The SUMMARY must include:
- The exact slash command invocation form discovered
- Whether `$CLAUDE_PLUGIN_ROOT` expansion works in SKILL.md Read paths
- The path prefix strategy to use in Plan 2
</output>
