---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/socrates/SKILL.md
  - .gitmodules
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "User types /socrates in Claude Code and the command appears with the argument hint '<describe your problem>'"
    - "The dialectics git submodule is initialized and .cue files are readable via the Read tool"
    - "SKILL.md instructs Claude to load protocol files on demand — no protocol content is inlined in SKILL.md"
    - "SKILL.md body is under 150 lines and contains only frontmatter, preflight check, no-arg intro, and file path references"
  artifacts:
    - path: ".claude/skills/socrates/SKILL.md"
      provides: "Skill entrypoint with frontmatter registration and progressive disclosure instructions"
      contains: "name: socrates"
    - path: ".gitmodules"
      provides: "Git submodule registration for dialectics"
      contains: "dialectics"
    - path: ".claude/skills/socrates/dialectics/dialectics.cue"
      provides: "Kernel primitives CUE file (proves submodule is initialized)"
  key_links:
    - from: ".claude/skills/socrates/SKILL.md"
      to: "protocols/*.opt.cue"
      via: "Read file path references in SKILL.md body"
      pattern: "protocols/"
    - from: ".claude/skills/socrates/SKILL.md"
      to: ".claude/skills/socrates/dialectics/dialectics.cue"
      via: "Preflight check reads submodule file to verify initialization"
      pattern: "protocols/dialectics.opt.cue"
---

<objective>
Register the `/socrates` slash command in Claude Code, wire the dialectics git submodule, and create the SKILL.md with progressive disclosure file structure.

Purpose: Establishes the foundational skill infrastructure that all subsequent phases depend on — without this, there is no slash command, no access to protocol files, and no progressive loading structure.
Output: A working `/socrates` command that responds to invocation (no-arg intro), verifies submodule health (preflight check), and has the file reference structure ready for protocol loading.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git submodule and skill directory structure</name>
  <files>.gitmodules, .claude/skills/socrates/dialectics/</files>
  <action>
    1. Create the skill directory: `mkdir -p .claude/skills/socrates/protocols`
    2. Add the dialectics git submodule inside the skill directory:
       ```bash
       git submodule add https://github.com/riverline-labs/dialectics.git .claude/skills/socrates/dialectics
       ```
    3. Initialize the submodule:
       ```bash
       git submodule update --init --recursive
       ```
    4. Verify the submodule is initialized by confirming these files exist:
       - `.claude/skills/socrates/dialectics/dialectics.cue`
       - `.claude/skills/socrates/dialectics/governance/routing.cue`
       - `.claude/skills/socrates/dialectics/governance/recording.cue`
       - `.claude/skills/socrates/dialectics/protocols/adversarial/` (6 .cue files)
       - `.claude/skills/socrates/dialectics/protocols/evaluative/` (6 .cue files)
       - `.claude/skills/socrates/dialectics/protocols/exploratory/adp.cue`
    5. Verify `.gitmodules` was created at repo root with the correct URL and path.

    Note: The submodule pins to HEAD at the time of `git submodule add`. This is the correct behavior per RESEARCH.md open question #3.
  </action>
  <verify>
    - `git submodule status` shows the dialectics submodule with a commit hash (no `-` prefix)
    - `ls .claude/skills/socrates/dialectics/dialectics.cue` succeeds
    - `ls .claude/skills/socrates/dialectics/protocols/adversarial/*.cue | wc -l` returns 6
    - `ls .claude/skills/socrates/dialectics/protocols/evaluative/*.cue | wc -l` returns 6
    - `ls .claude/skills/socrates/dialectics/protocols/exploratory/adp.cue` succeeds
    - `cat .gitmodules` shows the dialectics submodule entry
  </verify>
  <done>Git submodule is initialized, all 15 CUE files (13 protocols + 2 governance) are readable on disk, and `.gitmodules` is tracked</done>
</task>

<task type="auto">
  <name>Task 2: Create SKILL.md with frontmatter, preflight check, no-arg handler, and file references</name>
  <files>.claude/skills/socrates/SKILL.md</files>
  <action>
    Create `.claude/skills/socrates/SKILL.md` with the following structure:

    **Frontmatter (YAML):**
    ```yaml
    ---
    name: socrates
    description: Apply structured dialectic reasoning to any problem. Use when facing competing design candidates, argument stress-testing, assumption audits, causal claims, analogy evaluation, formalization, or possibility mapping. Accepts a problem description and auto-routes to the correct protocol.
    argument-hint: "<describe your problem>"
    disable-model-invocation: true
    allowed-tools: Read
    ---
    ```

    Per locked decisions:
    - `name: socrates` creates the `/socrates` command (INFRA-01)
    - `argument-hint` shows freeform problem description (INFRA-02). No flags hinted — flags are Phase 4.
    - `disable-model-invocation: true` prevents auto-triggering during conversation
    - `allowed-tools: Read` is all Phase 1 needs (read .cue files)

    **Body — Section 1: Preflight Check**
    First instruction in body. Read `protocols/dialectics.opt.cue` (the pre-stripped kernel primitives file). If file not found or empty, respond with a setup message telling the user to run `git submodule update --init --recursive` and stop. Do not proceed.

    This validates BOTH submodule initialization AND stripped file generation in one check.

    **Body — Section 2: No-Argument Handler**
    If `$ARGUMENTS` is empty or blank:
    Respond with the minimal intro per locked decision: "I apply structured dialectic reasoning to your problem — from testing assumptions to mapping possibility spaces. What would you like to reason through?"
    Stop. Do NOT list protocols or categories (locked decision).

    **Body — Section 3: Protocol File References (Progressive Disclosure)**
    List all protocol file paths organized by category. Claude reads ONLY the selected protocol file at routing time (Phase 2+). Phase 1 establishes the reference structure.

    Structure:
    - Kernel primitives: `protocols/dialectics.opt.cue`
    - Routing logic: `protocols/routing.opt.cue`
    - Adversarial protocols (6): `protocols/adversarial/{name}.opt.cue`
    - Evaluative protocols (6): `protocols/evaluative/{name}.opt.cue`
    - Exploratory protocol: `protocols/exploratory/adp.opt.cue`

    Include a clear instruction: "Read ONLY the protocol file for the selected protocol. Never load all protocols at once."

    **Body — Section 4: Execution Placeholder**
    A brief section noting that protocol routing and execution are implemented in Phase 2 and Phase 3 respectively. This prevents Claude from attempting to execute protocols before routing logic exists. Something like:
    "Protocol routing and execution are not yet implemented. If you reach this point, inform the user that the skill is in setup mode and protocol execution will be available in a future update."

    **Constraints:**
    - SKILL.md body MUST be under 150 lines (target 60-100 lines for Phase 1)
    - ZERO CUE content inlined — only file path references
    - All file paths use relative paths from the skill directory (e.g., `protocols/adversarial/cffp.opt.cue`)
  </action>
  <verify>
    - `wc -l .claude/skills/socrates/SKILL.md` is under 150 lines
    - File starts with `---` (YAML frontmatter)
    - `grep 'name: socrates' .claude/skills/socrates/SKILL.md` finds the name field
    - `grep 'argument-hint' .claude/skills/socrates/SKILL.md` finds the hint
    - `grep 'disable-model-invocation: true' .claude/skills/socrates/SKILL.md` confirms invocation control
    - `grep -c '\.opt\.cue' .claude/skills/socrates/SKILL.md` returns >= 15 (all protocol file references)
    - `grep 'ARGUMENTS' .claude/skills/socrates/SKILL.md` confirms no-arg handler exists
    - No CUE syntax (field definitions, types, constraints) inlined in body — only file path references
  </verify>
  <done>SKILL.md exists with correct frontmatter (name, argument-hint, disable-model-invocation, allowed-tools), preflight check, no-arg handler, and path references to all 15 protocol files. Body is under 150 lines with zero inlined CUE content.</done>
</task>

</tasks>

<verification>
1. `/socrates` command would appear in Claude Code (verify SKILL.md frontmatter has `name: socrates`)
2. Argument hint shows correct format (verify `argument-hint: "<describe your problem>"`)
3. Submodule is initialized and all CUE files readable (verify `git submodule status` and file existence)
4. Progressive disclosure structure in place (verify SKILL.md references `protocols/*.opt.cue` paths without inlining CUE content)
5. SKILL.md under 150 lines (verify with `wc -l`)
</verification>

<success_criteria>
- `.claude/skills/socrates/SKILL.md` exists with valid frontmatter containing all required fields
- `.claude/skills/socrates/dialectics/` contains the full dialectics submodule with all 15 CUE files
- `.gitmodules` tracks the dialectics submodule
- SKILL.md body references all 15 protocol file paths without inlining any CUE content
- SKILL.md body is under 150 lines
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
