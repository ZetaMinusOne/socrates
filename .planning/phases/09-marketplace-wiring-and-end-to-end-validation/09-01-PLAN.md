---
phase: 09-marketplace-wiring-and-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - socrates/.claude-plugin/plugin.json
  - scripts/strip_cue.py
  - Makefile
  - socrates/governance/recording.opt.cue
  - socrates/skills/socrates/SKILL.md
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02

must_haves:
  truths:
    - "marketplace.json at repo root makes zetaminusone/socrates a discoverable marketplace with a single plugin entry pointing to ./socrates"
    - "plugin.json has no version field — marketplace.json is the sole version authority for relative-path distribution"
    - "recording.opt.cue is pre-built and committed at socrates/governance/ so --record works without submodule init"
    - "make build generates 16 files (15 protocols + 1 governance) and make check validates all 16 are up to date"
    - "All SKILL.md protocol file paths use $CLAUDE_PLUGIN_ROOT/protocols/ (not $CLAUDE_PLUGIN_ROOT/socrates/protocols/) so reads resolve correctly after marketplace install"
    - "SKILL.md recording.cue path points to $CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue (pre-built file, not submodule path)"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog with socrates-skill plugin entry"
      contains: "socrates-marketplace"
    - path: "socrates/.claude-plugin/plugin.json"
      provides: "Plugin manifest without version field"
      contains: "socrates-skill"
    - path: "socrates/governance/recording.opt.cue"
      provides: "Pre-built recording schema for --record flag"
      contains: "#Record"
    - path: "socrates/skills/socrates/SKILL.md"
      provides: "Corrected $CLAUDE_PLUGIN_ROOT paths for marketplace install"
      contains: "$CLAUDE_PLUGIN_ROOT/protocols/"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "socrates/.claude-plugin/plugin.json"
      via: "source: ./socrates points marketplace to plugin directory"
      pattern: '"source".*"\\./socrates"'
    - from: "socrates/skills/socrates/SKILL.md"
      to: "socrates/protocols/"
      via: "$CLAUDE_PLUGIN_ROOT/protocols/ path prefix in Read instructions"
      pattern: "\\$CLAUDE_PLUGIN_ROOT/protocols/"
    - from: "socrates/skills/socrates/SKILL.md"
      to: "socrates/governance/recording.opt.cue"
      via: "$CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue path in --record handler"
      pattern: "\\$CLAUDE_PLUGIN_ROOT/governance/recording\\.opt\\.cue"
    - from: "scripts/strip_cue.py"
      to: "socrates/governance/recording.opt.cue"
      via: "FILE_MAP entry mapping recording.cue to governance/recording.opt.cue"
      pattern: "governance/recording\\.opt\\.cue"
---

<objective>
Create the marketplace catalog, extend the build pipeline for recording.cue distribution, fix the SKILL.md $CLAUDE_PLUGIN_ROOT path prefix bug, and remove the version override from plugin.json — making the repository fully marketplace-ready for end-to-end validation.

Purpose: This is the infrastructure preparation for Phase 9's E2E gate. Every file change here enables the real GitHub install test in Plan 02. The path prefix fix is the critical blocker — without it, all protocol file reads fail after marketplace install.

Output: marketplace.json (new), recording.opt.cue (new), modified plugin.json/strip_cue.py/Makefile/SKILL.md
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-marketplace-wiring-and-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-marketplace-wiring-and-end-to-end-validation/09-RESEARCH.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-02-SUMMARY.md
@.planning/phases/07-pre-built-protocol-files/07-01-SUMMARY.md

<interfaces>
<!-- Key structures the executor needs to modify. Extracted from codebase. -->

From scripts/strip_cue.py (FILE_MAP to extend):
```python
FILE_MAP = [
    ("dialectics/dialectics.cue",                          "protocols/dialectics.opt.cue"),
    ("dialectics/governance/routing.cue",                  "protocols/routing.opt.cue"),
    # ... 13 protocol files ...
    ("dialectics/protocols/exploratory/adp.cue",           "protocols/exploratory/adp.opt.cue"),
]
```

From Makefile (check target to extend):
```makefile
check:
	python3 scripts/strip_cue.py
	@git diff --exit-code socrates/protocols/ > /dev/null 2>&1 && echo "Protocol files are up to date." || (echo "Protocol files are stale — run 'make build' and commit the changes."; exit 1)
```

From socrates/.claude-plugin/plugin.json (version to remove):
```json
{
  "name": "socrates-skill",
  "version": "0.1.0",
  ...
}
```

From SKILL.md (path pattern to fix — 23 occurrences):
```
$CLAUDE_PLUGIN_ROOT/socrates/protocols/dialectics.opt.cue
$CLAUDE_PLUGIN_ROOT/socrates/protocols/routing.opt.cue
$CLAUDE_PLUGIN_ROOT/socrates/protocols/adversarial/{acronym}.opt.cue
... (22 total with socrates/protocols/)
$CLAUDE_PLUGIN_ROOT/socrates/dialectics/governance/recording.cue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json, fix plugin.json version, extend build pipeline for recording.cue</name>
  <files>.claude-plugin/marketplace.json, socrates/.claude-plugin/plugin.json, scripts/strip_cue.py, Makefile, socrates/governance/recording.opt.cue</files>
  <action>
Four changes plus one generated file:

**1. Create `.claude-plugin/marketplace.json` at repo root (NEW file)**

The `.claude-plugin/` directory already exists at repo root (it was created for some prior purpose). Create `marketplace.json` inside it with this exact content:

```json
{
  "name": "socrates-marketplace",
  "owner": {
    "name": "zetaminusone"
  },
  "plugins": [
    {
      "name": "socrates-skill",
      "source": "./socrates",
      "description": "Apply rigorous philosophical reasoning to any problem — 13 protocols for stress-testing arguments, auditing assumptions, and mapping possibilities",
      "version": "0.1.0",
      "homepage": "https://zetaminusone.com",
      "repository": "https://github.com/zetaminusone/socrates",
      "tags": ["reasoning", "dialectics", "philosophy"]
    }
  ]
}
```

Per CONTEXT.md locked decisions: marketplace name `socrates-marketplace`, owner `zetaminusone`, relative path `./socrates`, description as specified, tags `reasoning/dialectics/philosophy`, version `0.1.0` in marketplace entry.

**2. Remove `version` from `socrates/.claude-plugin/plugin.json`**

Delete the `"version": "0.1.0"` line from plugin.json. The resulting file must be valid JSON with no trailing comma artifacts. Per CONTEXT.md: "Version lives in marketplace.json only (per official docs)." If plugin.json keeps version, it silently overrides the marketplace version, breaking update detection.

After edit, plugin.json should be:
```json
{
  "name": "socrates-skill",
  "description": "Structured dialectic reasoning via 13 protocols. Invoke explicitly with /socrates — never auto-applies to problems.",
  "author": {
    "name": "zetaminusone"
  },
  "homepage": "https://zetaminusone.com",
  "repository": "https://github.com/riverline-labs/socrates",
  "license": "MIT"
}
```

**3. Extend `scripts/strip_cue.py` FILE_MAP**

Add one entry to FILE_MAP for recording.cue. Insert it after the routing.cue entry (logical grouping: governance files together):

```python
("dialectics/governance/recording.cue",                "governance/recording.opt.cue"),
```

The script's `os.makedirs(os.path.dirname(dst_path), exist_ok=True)` in `process_file()` will automatically create the `socrates/governance/` directory. No other changes to strip_cue.py are needed.

**4. Extend `Makefile` check target**

The `check` target currently validates only `socrates/protocols/`. Add `socrates/governance/` to the git diff check:

Change:
```makefile
@git diff --exit-code socrates/protocols/ > /dev/null 2>&1 && echo "Protocol files are up to date." || (echo "Protocol files are stale — run 'make build' and commit the changes."; exit 1)
```

To:
```makefile
@git diff --exit-code socrates/protocols/ socrates/governance/ > /dev/null 2>&1 && echo "Protocol files are up to date." || (echo "Protocol files are stale — run 'make build' and commit the changes."; exit 1)
```

Also update the `clean` target to include the governance directory:
```makefile
clean:
	find socrates/protocols socrates/governance -name '*.opt.cue' -delete
```

**5. Generate recording.opt.cue**

Run `make build` from the repo root. This runs `strip_cue.py` which now includes the recording.cue entry. Verify the output file exists at `socrates/governance/recording.opt.cue` and contains `#Record`.

After `make build`, verify the count: `find socrates/protocols socrates/governance -name '*.opt.cue' | wc -l` should return 16.
  </action>
  <verify>
    <automated>make build && test -f .claude-plugin/marketplace.json && test -f socrates/governance/recording.opt.cue && python3 -c "import json; m=json.load(open('.claude-plugin/marketplace.json')); assert m['name']=='socrates-marketplace'; p=json.load(open('socrates/.claude-plugin/plugin.json')); assert 'version' not in p; print('MARKETPLACE_AND_PLUGIN_OK')" && count=$(find socrates/protocols socrates/governance -name '*.opt.cue' | wc -l | tr -d ' ') && [ "$count" = "16" ] && echo "FILE_COUNT_OK_16"</automated>
  </verify>
  <done>marketplace.json exists with correct schema at repo root, plugin.json has no version field, strip_cue.py generates 16 files (15 protocols + recording), Makefile check validates all 16, socrates/governance/recording.opt.cue exists and contains #Record</done>
</task>

<task type="auto">
  <name>Task 2: Fix SKILL.md $CLAUDE_PLUGIN_ROOT path prefix for marketplace install compatibility</name>
  <files>socrates/skills/socrates/SKILL.md</files>
  <action>
Fix the critical path prefix bug: all 23 `$CLAUDE_PLUGIN_ROOT/socrates/` path references in SKILL.md must be corrected for marketplace install compatibility.

**Root cause:** When installed via marketplace, `$CLAUDE_PLUGIN_ROOT` resolves to the plugin directory itself (`socrates/`). So `$CLAUDE_PLUGIN_ROOT/socrates/protocols/` becomes `socrates/socrates/protocols/` — a nonexistent doubled path. The fix is to remove the `/socrates` segment from all paths.

**Mechanical substitution (22 occurrences):**

Replace ALL occurrences of `$CLAUDE_PLUGIN_ROOT/socrates/protocols/` with `$CLAUDE_PLUGIN_ROOT/protocols/` throughout SKILL.md.

These appear in:
- Line 11: preflight read path
- Line 40: "Optimized protocol files are in..." description
- Lines 43-44: kernel/routing paths
- Lines 47-52: 6 adversarial protocol paths
- Lines 55-60: 6 evaluative protocol paths
- Line 63: 1 exploratory protocol path
- Line 67: routing step path
- Line 178: adversarial execution read instruction
- Line 219: evaluative execution read instruction
- Line 235: exploratory execution read instruction

**Special substitution (1 occurrence — line 36):**

The recording.cue path changes BOTH prefix AND destination:

Replace:
```
$CLAUDE_PLUGIN_ROOT/socrates/dialectics/governance/recording.cue
```
With:
```
$CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue
```

This simultaneously fixes the path prefix AND points to the pre-built file (from Task 1) instead of the submodule source.

**Also update line 300** which has the second recording.cue reference:

Replace:
```
$CLAUDE_PLUGIN_ROOT/socrates/dialectics/governance/recording.cue
```
With:
```
$CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue
```

**Post-edit verification:**
After all substitutions, verify zero occurrences remain:
- `grep -c 'CLAUDE_PLUGIN_ROOT/socrates/' socrates/skills/socrates/SKILL.md` must return `0`
- `grep -c 'dialectics/governance/recording.cue' socrates/skills/socrates/SKILL.md` must return `0`
- `grep -c 'CLAUDE_PLUGIN_ROOT/protocols/' socrates/skills/socrates/SKILL.md` should return `22` (the corrected paths)
- `grep -c 'CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue' socrates/skills/socrates/SKILL.md` should return `2` (lines 36 and 300)

Do NOT change any other content in SKILL.md — this is a path-only fix.
  </action>
  <verify>
    <automated>old_count=$(grep -c 'CLAUDE_PLUGIN_ROOT/socrates/' socrates/skills/socrates/SKILL.md || true) && new_count=$(grep -c 'CLAUDE_PLUGIN_ROOT/protocols/' socrates/skills/socrates/SKILL.md || true) && rec_count=$(grep -c 'CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue' socrates/skills/socrates/SKILL.md || true) && [ "$old_count" = "0" ] && [ "$new_count" = "22" ] && [ "$rec_count" = "2" ] && echo "PATH_FIX_VERIFIED"</automated>
  </verify>
  <done>Zero occurrences of $CLAUDE_PLUGIN_ROOT/socrates/ remain in SKILL.md, 22 corrected protocol paths use $CLAUDE_PLUGIN_ROOT/protocols/, 2 recording references use $CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue, no content changes outside path references</done>
</task>

</tasks>

<verification>
All Plan 01 checks:

1. `.claude-plugin/marketplace.json` exists at repo root with `name: "socrates-marketplace"`, `owner.name: "zetaminusone"`, `plugins[0].name: "socrates-skill"`, `plugins[0].source: "./socrates"`, `plugins[0].version: "0.1.0"`
2. `socrates/.claude-plugin/plugin.json` has NO `"version"` key — `grep -c '"version"' socrates/.claude-plugin/plugin.json` returns `0`
3. `socrates/governance/recording.opt.cue` exists and contains `#Record`
4. `make build` generates 16 files; `make check` reports up-to-date
5. `grep -c 'CLAUDE_PLUGIN_ROOT/socrates/' socrates/skills/socrates/SKILL.md` returns `0` — path prefix bug eliminated
6. `grep -c 'CLAUDE_PLUGIN_ROOT/protocols/' socrates/skills/socrates/SKILL.md` returns `22` — corrected paths in place
7. `grep -c 'CLAUDE_PLUGIN_ROOT/governance/recording.opt.cue' socrates/skills/socrates/SKILL.md` returns `2` — recording path updated
8. `grep -c 'dialectics/governance/recording.cue' socrates/skills/socrates/SKILL.md` returns `0` — no submodule path references remain
</verification>

<success_criteria>
- marketplace.json valid JSON with all required fields per CONTEXT.md locked decisions
- plugin.json version removed; marketplace.json is sole version authority
- recording.opt.cue pre-built via extended strip_cue.py pipeline
- Makefile check validates 16 files (protocols/ + governance/)
- All 23 SKILL.md path occurrences corrected: zero `$CLAUDE_PLUGIN_ROOT/socrates/` remaining
- Recording.cue paths point to pre-built governance/recording.opt.cue, not submodule
</success_criteria>

<output>
After completion, create `.planning/phases/09-marketplace-wiring-and-end-to-end-validation/09-01-SUMMARY.md`
</output>
