---
phase: 05-schema-conformance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/socrates/SKILL.md
autonomous: true
requirements:
  - EXEC-05
  - OUTP-02
  - OUTP-03

must_haves:
  truths:
    - "CDP skip-retry terminal path uses `close_as_unified` resolution enum value"
    - "ATP skip-retry terminal path uses `close_as_rejected` resolution enum value"
    - "ADP + `--record` flow handles missing `#Protocol.version` gracefully with `\"n/a\"` fallback"
    - "AAP fragility map tier labels match schema: `structural`, `significant`, `moderate`, `minor`"
    - "Structured output section references `#{ACRONYM}Instance` pattern with explicit `#ADPRecord` exception"
    - "No SKILL.md enum value, type reference, or tier label contradicts its source `.opt.cue` definition"
  artifacts:
    - path: ".claude/skills/socrates/SKILL.md"
      provides: "Corrected execution and output rendering instructions"
      contains: "close_as_unified"
  key_links:
    - from: ".claude/skills/socrates/SKILL.md"
      to: "protocols/adversarial/cdp.opt.cue"
      via: "skip-retry resolution enum"
      pattern: "close_as_unified"
    - from: ".claude/skills/socrates/SKILL.md"
      to: "protocols/adversarial/atp.opt.cue"
      via: "skip-retry resolution enum"
      pattern: "close_as_rejected"
    - from: ".claude/skills/socrates/SKILL.md"
      to: "protocols/evaluative/aap.opt.cue"
      via: "FragilityTier tier labels"
      pattern: "structural.*significant.*moderate.*minor"
    - from: ".claude/skills/socrates/SKILL.md"
      to: "protocols/exploratory/adp.opt.cue"
      via: "instance type name and version fallback"
      pattern: "ADPRecord"
---

<objective>
Fix all schema-instruction mismatches in SKILL.md so that `--structured` and `--record` output produces valid enum values, correct type references, and accurate tier labels.

Purpose: The v1.0 audit identified 4 gaps (plus 1 found during full sweep) where SKILL.md hard-codes incorrect enum values, type names, or tier labels that don't match the actual CUE schema definitions. These mismatches cause `--structured` output to emit invalid values and `--record` to fail for ADP.

Output: Corrected SKILL.md with all 5 fixes applied and verified against source schemas.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-schema-conformance/05-RESEARCH.md
@.planning/phases/05-schema-conformance/05-CONTEXT.md
@.claude/skills/socrates/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all 5 schema-instruction fixes to SKILL.md</name>
  <files>.claude/skills/socrates/SKILL.md</files>
  <action>
Make 5 targeted text edits to SKILL.md. Each edit corrects a schema-instruction mismatch identified in 05-RESEARCH.md. Apply edits in file order (by line number).

**Fix 1 — Resolution enum values (line 199, Phase 3b skip-retry):**

Find the sentence:
"Apply the `reframe_and_close` resolution, explain why retrying won't help, and give the user reframe suggestions. Stop."

Replace with:
"Apply the protocol's skip-retry resolution — `reframe_and_close` for CFFP, `close_as_unified` for CDP, `close_as_rejected` for ATP — explain why retrying won't help, and give the user reframe suggestions. Stop."

This corrects EXEC-05: CDP and ATP terminal paths now use their schema-defined resolution enum values instead of CFFP's `reframe_and_close`.

**Fix 2 — AAP tier labels (line 231, evaluative protocols AAP note):**

Find the parenthetical:
"(load-bearing, structural, background)"

Replace with:
": Tier 1 (`structural`), Tier 2 (`significant`), Tier 3 (`moderate`), Tier 4 (`minor`)"

This corrects the 3 wrong/missing tier labels to match `#FragilityTier.label` enum values from aap.opt.cue. Affects OUTP-02 (structured output must emit valid tier values).

**Fix 3 — Protocol instance type reference (line 283, structured output section):**

Find:
"Field names and nesting follow the protocol's `#ProtocolInstance` type from its `.opt.cue` file."

Replace with:
"Field names and nesting follow the protocol's instance type from its `.opt.cue` file: `#{ACRONYM}Instance` for all protocols except ADP, which uses `#ADPRecord`."

This corrects Issue 4 and Issue 5 together: replaces the non-existent `#ProtocolInstance` generic with the actual naming pattern plus the ADP exception. Affects OUTP-02.

**Fix 4 — ADP version fallback for --record (line 308, record output section):**

Find:
"- `source_run.run_version`: From `#Protocol.version` in the loaded `.opt.cue` file"

Replace with:
"- `source_run.run_version`: From `#Protocol.version` in the loaded `.opt.cue` file. If the protocol has no `#Protocol` type (currently: ADP only), use `\"n/a\"` and note this in the `notes` field."

This corrects Issue 2: ADP has no `#Protocol` type, so the version field instruction needs a fallback. Uses `"n/a"` which is type-valid for `run_version: string`. Affects OUTP-03.

**No other changes.** The full sweep in 05-RESEARCH.md confirmed these are the only 5 issues (4 original audit + 1 found during sweep). Do NOT modify: gate field names (correct), outcome enums (correct/schema-directed), dispute kind table (correct), phase counts (correct), ADP round types (correct), routing enums (correct).
  </action>
  <verify>
Read the modified SKILL.md and confirm:
1. Line containing "skip retry entirely" now lists three distinct resolution values: `reframe_and_close`, `close_as_unified`, `close_as_rejected`
2. AAP note line contains all 4 tier labels: `structural`, `significant`, `moderate`, `minor` — and does NOT contain `load-bearing` or `background`
3. Structured output section references `#{ACRONYM}Instance` pattern and explicitly names `#ADPRecord`
4. Record output section includes the fallback rule for protocols without `#Protocol` type
  </verify>
  <done>All 5 fixes applied. SKILL.md contains correct resolution enums per protocol, correct AAP tier labels from schema, correct instance type pattern with ADP exception, and ADP version fallback rule for --record.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-verify all fixes against source CUE schemas</name>
  <files>.claude/skills/socrates/SKILL.md</files>
  <action>
Read each source `.opt.cue` file referenced by the fixes and confirm the corrected SKILL.md values match exactly:

1. Read `protocols/adversarial/cffp.opt.cue` — confirm `reframe_and_close` is a valid Phase3b resolution value
2. Read `protocols/adversarial/cdp.opt.cue` — confirm `close_as_unified` is a valid Phase3b resolution value
3. Read `protocols/adversarial/atp.opt.cue` — confirm `close_as_rejected` is a valid Phase3b resolution value
4. Read `protocols/evaluative/aap.opt.cue` — confirm `#FragilityTier.label` enum contains exactly `structural`, `significant`, `moderate`, `minor`
5. Read `protocols/exploratory/adp.opt.cue` — confirm no `#Protocol` type exists and the top-level output type is `#ADPRecord` (not `#ADPInstance`)
6. Read `dialectics/governance/recording.cue` — confirm `run_version: string` accepts any string (so `"n/a"` is valid)

For each check, verify the SKILL.md instruction now matches the schema. If any mismatch is found, fix it immediately.

**Full sweep confirmation:** After verifying the 5 fixes, do a final pass over SKILL.md to confirm no OTHER enum values, type references, or tier labels contradict their source schemas. The research already confirmed no additional issues exist, but this is the belt-and-suspenders verification pass.
  </action>
  <verify>
For each of the 6 schema reads above, the value in SKILL.md matches the schema definition exactly. The full sweep pass finds zero additional mismatches.
  </verify>
  <done>All 5 corrected values in SKILL.md are confirmed against their source `.opt.cue` schemas. Full sweep verification pass finds no remaining mismatches. SKILL.md instructions now produce valid enum values, correct type references, and accurate tier labels for all 13 protocols.</done>
</task>

</tasks>

<verification>
1. Read SKILL.md and confirm all 5 fixes are present
2. Cross-reference each fix against its source `.opt.cue` schema file
3. Confirm no regressions: gate field names, outcome enums, dispute kind table, routing logic, and all other SKILL.md instructions remain unchanged
4. Verify the file is valid markdown (no broken formatting from edits)
</verification>

<success_criteria>
- CDP terminal path uses `close_as_unified` (not `reframe_and_close`)
- ATP terminal path uses `close_as_rejected` (not `reframe_and_close`)
- CFFP terminal path still uses `reframe_and_close` (unchanged, was already correct)
- AAP tier labels are `structural`, `significant`, `moderate`, `minor` (not `load-bearing`, `structural`, `background`)
- Structured output references `#{ACRONYM}Instance` with explicit `#ADPRecord` exception
- Record output `source_run.run_version` has fallback rule for protocols without `#Protocol` type
- Full sweep confirms zero remaining mismatches across all 13 protocols
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-conformance/05-01-SUMMARY.md`
</output>
