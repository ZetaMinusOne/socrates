---
phase: 02-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/socrates/SKILL.md
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03

must_haves:
  truths:
    - "User describes a problem and the skill selects the correct protocol without the user naming any protocol"
    - "Every invocation shows which protocol was selected, with acronym, full name, detected structural features, and a one-sentence rationale"
    - "When a problem maps to multiple protocols with a prerequisite relationship, the skill shows the full sequence before execution"
    - "When routing is ambiguous, the skill asks for clarification using plain language (never protocol names)"
    - "When no structural features are detected, the skill explains what kinds of problems it handles and suggests rephrasing"
  artifacts:
    - path: ".claude/skills/socrates/SKILL.md"
      provides: "Complete routing logic — feature extraction, protocol selection, display, and all three outcome handlers"
      contains: "## Routing"
  key_links:
    - from: "SKILL.md ## Routing"
      to: "protocols/routing.opt.cue"
      via: "Read tool instruction to load routing schema at invocation time"
      pattern: "Read.*routing\\.opt\\.cue"
    - from: "SKILL.md ## Routing"
      to: "#StructuralFeature inline comments"
      via: "Feature-to-protocol mapping extracted from schema enum comments"
      pattern: "structural feature.*protocol"
    - from: "SKILL.md ## Routing outcome: routed"
      to: "SKILL.md ## Execution"
      via: "Routing block displayed then execution proceeds immediately"
      pattern: "proceed.*execution|Phase 3"
---

<objective>
Implement auto-routing in the Socrates skill so that users describe any problem and the skill transparently selects the correct dialectic protocol before execution begins.

Purpose: This is the core intelligence of the skill — without routing, users would need to know which of 13 protocols to invoke. Routing makes the skill accessible to anyone.
Output: Updated SKILL.md with complete Routing section replacing the "setup mode" placeholder, handling all three outcomes (routed, ambiguous, unroutable) and composite sequencing.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routing/02-CONTEXT.md
@.planning/phases/02-routing/02-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.claude/skills/socrates/SKILL.md
@.claude/skills/socrates/protocols/routing.opt.cue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add routing logic to SKILL.md — feature extraction, protocol selection, and boundary discrimination</name>
  <files>.claude/skills/socrates/SKILL.md</files>
  <action>
Modify SKILL.md to add the core routing logic. This involves two changes:

**Change 1: Fix incorrect protocol names in the Protocol Files section (lines 34-52)**

The current Protocol Files section has wrong full names for several protocols. Replace with the actual names from the protocol .opt.cue files:

- `atp.opt.cue` — Analogy Transfer Protocol (currently says "Assumption Testing Protocol")
- `cbp.opt.cue` — Concept Boundary Protocol (currently says "Challenge-Based Protocol")
- `cdp.opt.cue` — Construct Decomposition Protocol (currently says "Counter-Dialectic Protocol")
- `cffp.opt.cue` — Constraint-First Formalization Protocol (currently says "Claim-Flaw-Fix Protocol")
- `emp.opt.cue` — Emergence Mapping Protocol (currently says "Epistemological Mapping Protocol")
- `hep.opt.cue` — Hypothesis Elimination Protocol (correct)
- `aap.opt.cue` — Assumption Audit Protocol (currently says "Argument Assessment Protocol")
- `cgp.opt.cue` — Canonical Governance Protocol (currently says "Comparative Grounding Protocol")
- `ifa.opt.cue` — Implementation Fidelity Audit (currently says "Inference Fidelity Assessment")
- `ovp.opt.cue` — Observation Validation Protocol (currently says "Option Viability Protocol")
- `ptp.opt.cue` — Prioritization Triage Protocol (currently says "Position Testing Protocol")
- `rcp.opt.cue` — Reconciliation Protocol (currently says "Reasoning Chain Protocol")
- `adp.opt.cue` — Adversarial Design Protocol (currently says "Analytic Decomposition Protocol")

**Change 2: Add the `## Routing` section immediately after the `## Protocol Files` section and before the `## Execution` section**

The Routing section must contain these subsections in order:

**Protocol full names lookup table:**
```
- AAP: Assumption Audit Protocol
- ADP: Adversarial Design Protocol
- ATP: Analogy Transfer Protocol
- CBP: Concept Boundary Protocol
- CDP: Construct Decomposition Protocol
- CFFP: Constraint-First Formalization Protocol
- CGP: Canonical Governance Protocol
- EMP: Emergence Mapping Protocol
- HEP: Hypothesis Elimination Protocol
- IFA: Implementation Fidelity Audit
- OVP: Observation Validation Protocol
- PTP: Prioritization Triage Protocol
- RCP: Reconciliation Protocol
```

**Step 1: Read routing schema**
Instruct Claude to read `protocols/routing.opt.cue` to load `#StructuralFeature`, `#FeatureProtocolMapping`, `#DisambiguationRule`, and `#RoutingResult` type definitions.

**Step 2: Extract structural features**
Instruct Claude to analyze the user's problem in `$ARGUMENTS` and identify which `#StructuralFeature` values are present. The feature-to-protocol mapping is in the inline comments of the `#StructuralFeature` enum (e.g., `"term_inconsistency" | // term used differently across contexts → CBP`). Claude uses these comments as the authoritative routing table.

**Step 3: Apply boundary discrimination**
When the problem could match multiple features, Claude must apply these discriminating questions INTERNALLY (not shown to user) to narrow to one:

- **OVP vs HEP** (observation_validity vs causal_ambiguity): "Is the observation itself questionable, or is the phenomenon clearly real but the cause unclear?" — If observation is questioned → OVP; if phenomenon is real but cause unclear → HEP.
- **CBP vs CDP** (term_inconsistency vs construct_incoherence): "Is the same word being used differently across contexts/people, or is one concept internally splitting into two different things?" — Cross-context disagreement → CBP; internal split → CDP.
- **CFFP vs PTP** (competing_candidates vs resource_constrained_choice): "Are the options in genuine tension (adopting one means the other fails), or are all options valid and the constraint is capacity/resources?" — Genuine tension → CFFP; capacity constraint → PTP.
- **CFFP vs CGP** (competing_candidates vs revision_pressure): "Is there an existing canonical form being proposed for change, or are these competing new proposals with no established baseline?" — Existing baseline → CGP; no baseline → CFFP.
- **AAP vs RCP** (argument_fragility vs cross_run_conflict): "Is there one argument being stress-tested, or two independent reasoning sessions that reached different conclusions?" — One argument → AAP; two sessions conflicting → RCP.

**Step 4: Determine outcome**
Instruct Claude to determine the `#RoutingResult.outcome`:
- `"routed"` — one clear protocol selected (or one clear protocol after disambiguation)
- `"ambiguous"` — multiple features detected, no discrimination question resolves them, features point to different protocols
- `"unroutable"` — no structural features detected in the problem description

**Step 5: Check for composite sequencing**
If `observation_validity` AND `causal_ambiguity` co-occur, set `sequenced: true` and build the OVP → HEP sequence. This is the only confirmed prerequisite composite. For any other co-occurring features where one protocol has `prerequisites` in its `#FeatureProtocolMapping`, check if the prerequisite protocol is also indicated and sequence if so. Default: single protocol unless prerequisites are explicitly indicated.

Keep the routing logic section to approximately 60-80 lines of instructions. Be concise — one instruction per decision, not explanatory prose.
  </action>
  <verify>
Read the modified SKILL.md. Verify:
1. All 13 protocol names in the Protocol Files section match the actual `#Protocol.name` fields from the .opt.cue files
2. The `## Routing` section exists between `## Protocol Files` and `## Execution`
3. The protocol name lookup table lists all 13 protocols with correct full names
4. Steps 1-5 are present with feature extraction, boundary discrimination, outcome determination, and composite sequencing
5. Total SKILL.md body is under 250 lines
  </verify>
  <done>SKILL.md contains a Routing section with feature extraction instructions, the 14→protocol mapping via inline comments, 5 boundary discrimination questions, outcome determination logic, and composite sequencing rules. All protocol names in Protocol Files section are correct.</done>
</task>

<task type="auto">
  <name>Task 2: Add routing display handlers and replace Execution placeholder</name>
  <files>.claude/skills/socrates/SKILL.md</files>
  <action>
Continue modifying SKILL.md to add the display handlers for all three routing outcomes and composite sequencing, then replace the Execution placeholder.

**Add to the Routing section (after the step 5 logic from Task 1):**

**Display: Routed (single protocol)**
Per locked decision from CONTEXT.md, show a structured block visually separated by horizontal rules:
```
---
Protocol: {ACRONYM} ({Full Name})
Detected: {plain language description} ({schema_identifier})
Rationale: {One sentence explaining why this protocol fits the problem}
---
```
- Show confidence level ONLY when medium or low (not for high)
- Show warnings from the schema's `warnings` field ONLY when they would meaningfully change the user's interpretation (Claude's discretion)
- After the routing block, proceed immediately to execution — NO user confirmation gate, NO pause
- Do NOT show: disambiguation rules applied, features considered but not matched, alternative protocols not chosen

**Display: Routed (composite sequence)**
Per locked decision from CONTEXT.md, show the full sequence upfront:
```
---
Multi-protocol sequence:

1. {ACRONYM} ({Full Name}) — {purpose}
   Feeds into: {what this step's output provides to the next step}

2. {ACRONYM} ({Full Name}) — {purpose}
   Feeds into: conclusion

Detected: {plain language} ({schema_ids})
---

Starting with {first protocol}...
```
- Early termination: if step N produces an outcome that invalidates step N+1, explain the invalidation and stop. Do not continue unnecessary protocols.
- Example: if OVP returns "artifact" (phenomenon is not real), HEP would operate on a false premise — stop and explain.

**Display: Ambiguous**
Per locked decision: ask for clarification using plain language description of the fork. NEVER use protocol names in the clarification question.
```
Your problem could go in two directions:
- {Plain language description of direction A}
- {Plain language description of direction B}

Which describes your situation more accurately?
```
After the user clarifies, re-run routing with the additional context. If still ambiguous after one clarification attempt, Claude uses judgment on whether to ask again or pick the closer fit (Claude's discretion on retry limits).

**Display: Unroutable**
Per locked decision: explain without jargon, show what kinds of problems the skill handles, suggest rephrasing.
```
I couldn't identify a clear problem structure from your description.

Socrates handles problems like:
- "Why did X happen?" (causal investigation)
- "Which of these designs should I use?" (competing candidates)
- "Is this argument sound?" (assumption stress-testing)
- "I don't know what options exist" (design space exploration)

Could you describe what you're trying to figure out or decide?
```
Never force a bad fit — an unroutable problem gets the unroutable handler, not a randomly chosen protocol.

**Replace the `## Execution` section placeholder:**
Replace the current placeholder text ("Protocol routing and execution are not yet implemented...") with:
```
## Execution

Protocol execution will be implemented in a future update. After routing completes with outcome "routed", inform the user:
"Protocol {ACRONYM} ({Full Name}) has been selected. Execution is not yet available — it will be added in a future update."
```

Keep the display handlers concise — aim for approximately 50-70 lines total for all four handlers plus the updated Execution section. The entire SKILL.md body should stay under 250 lines after this task.
  </action>
  <verify>
Read the completed SKILL.md. Verify:
1. Routed display shows the exact block format from CONTEXT.md locked decisions (protocol acronym + full name, detected features with plain language + schema identifier, rationale)
2. Composite sequence display shows numbered steps with purpose and feeds-into relationships
3. Ambiguous handler asks for clarification in plain language (no protocol names)
4. Unroutable handler shows problem types the skill handles and suggests rephrasing
5. The old "setup mode" Execution placeholder is replaced with a Phase 3 stub
6. No user confirmation gate between routing display and execution handoff
7. Total SKILL.md body is under 250 lines
8. Count total lines to confirm budget (target: 150-200 lines body, must be under 250)
  </verify>
  <done>SKILL.md contains all three outcome display handlers (routed, ambiguous, unroutable) plus composite sequencing display, all matching CONTEXT.md locked decisions. The Execution placeholder is replaced with a Phase 3 handoff stub. Total SKILL.md body is under 250 lines.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Read the full SKILL.md and verify it follows the flow: Preflight → Input → Protocol Files → Routing → Execution
2. Verify the routing section references `protocols/routing.opt.cue` (not raw dialectics/ paths) per Phase 1 decision
3. Verify all 13 protocol names in both the Protocol Files section and the Routing lookup table are correct and consistent with each other
4. Verify no protocol names appear in the ambiguous clarification handler
5. Verify routing block proceeds directly to execution with no user confirmation gate
6. Count total SKILL.md body lines — must be under 250
7. Check that the composite sequencing logic documents OVP → HEP as the confirmed prerequisite relationship
</verification>

<success_criteria>
- SKILL.md contains a complete `## Routing` section with feature extraction, boundary discrimination, and all three outcome handlers
- A user describing "Why did my server crash? It could be a memory leak or a race condition" would trigger `causal_ambiguity` → HEP routing
- A user describing "Is my argument that X causes Y actually solid?" would trigger `argument_fragility` → AAP routing
- A user describing "something happened but I'm not sure it's real" would trigger `observation_validity` → OVP routing
- A user describing a problem with no structural features gets the unroutable handler with rephrasing suggestions
- The routing block format matches the CONTEXT.md locked decision exactly
- Total SKILL.md body stays under 250 lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing/02-01-SUMMARY.md`
</output>
