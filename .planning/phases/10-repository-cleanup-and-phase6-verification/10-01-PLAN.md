---
phase: 10-repository-cleanup-and-phase6-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - PLUG-03
  - PLUG-04
  - PATH-01
  - PATH-02
  - PATH-03

must_haves:
  truths:
    - "git ls-files --stage socrates/dialectics returns a gitlink entry (mode 160000) with the correct submodule SHA"
    - "git ls-files .claude/skills/socrates returns 0 entries — all 18 old tracked paths removed from HEAD"
    - "git status shows no unstaged deletes under .claude/skills/socrates/"
    - "06-VERIFICATION.md exists in Phase 6 directory confirming all 5 requirements (PLUG-03, PLUG-04, PATH-01, PATH-02, PATH-03) are satisfied"
    - "ROADMAP.md shows Phase 6 as Complete with both plan checkboxes checked"
  artifacts:
    - path: ".planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md"
      provides: "Formal verification of Phase 6 requirements with evidence cross-references"
      contains: "status: passed"
    - path: ".planning/ROADMAP.md"
      provides: "Updated progress table reflecting Phase 6 completion"
      contains: "Complete | 2026-03-01"
  key_links:
    - from: "06-VERIFICATION.md"
      to: "06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-UAT.md"
      via: "Evidence cross-references for PLUG-03, PLUG-04, PATH-01, PATH-02, PATH-03"
      pattern: "06-01-SUMMARY|06-02-SUMMARY|06-UAT"
    - from: "git index (socrates/dialectics gitlink)"
      to: ".gitmodules (socrates/dialectics path)"
      via: "git submodule registration"
      pattern: "160000.*socrates/dialectics"
---

<objective>
Fix the git index to register the submodule gitlink at the correct path, remove all old tracked paths from HEAD, create Phase 6 VERIFICATION.md to formally close 5 requirements, and update ROADMAP.md to reflect Phase 6 completion.

Purpose: Close the 3 audit-identified gaps (INTEG-01, INTEG-02, verification gap) so that downstream phases (8, 9) have a clean repository state and all Phase 6 requirements count as formally satisfied.

Output: Clean git index (gitlink at `socrates/dialectics`, zero old `.claude/` paths), `06-VERIFICATION.md`, updated `ROADMAP.md`.
</objective>

<execution_context>
@/Users/javier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/javier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-repository-cleanup-and-phase6-verification/10-RESEARCH.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-01-SUMMARY.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-02-SUMMARY.md
@.planning/phases/06-plugin-scaffold-and-path-migration/06-UAT.md
@.planning/phases/07-pre-built-protocol-files/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix git index — register gitlink at correct path and remove old tracked paths</name>
  <files>(git index only — no on-disk files created or modified)</files>
  <action>
This task fixes two audit-identified integration gaps (INTEG-01 and INTEG-02) in the git index. All operations are index-only — no files on disk are created or modified.

**Step 1: Capture the current gitlink SHA**

Run `git ls-files --stage | grep 160000` and extract the SHA. It should be `10528fb0206418c6fa204d0e9bf0f652acf23e5f` at path `.claude/skills/socrates/dialectics`. Store this SHA — you need it in Step 3.

**Step 2: Remove old gitlink from index (INTEG-01 part 1)**

```bash
git rm --cached ".claude/skills/socrates/dialectics"
```

This removes the gitlink entry at the old path. Use `--cached` because the file does not exist on disk at this path.

**Step 3: Register gitlink at correct path (INTEG-01 part 2)**

```bash
git update-index --add --cacheinfo 160000,<SHA_FROM_STEP_1>,socrates/dialectics
```

Replace `<SHA_FROM_STEP_1>` with the actual SHA captured in Step 1 (e.g., `10528fb0206418c6fa204d0e9bf0f652acf23e5f`). This creates the gitlink entry at `socrates/dialectics` which matches `.gitmodules`.

**Step 4: Remove remaining 17 old tracked files (INTEG-02)**

After the gitlink is removed (Step 2), remove the remaining 17 regular files:

```bash
git rm --cached -r ".claude/skills/socrates/"
```

All these files were deleted from disk during Phase 6 but never `git rm`'d. They appear as unstaged deletes in `git status`. The `--cached` flag is safe — nothing exists on disk to delete.

**Step 5: Verify clean state**

Run these verification checks:

```bash
# Verify gitlink at new path
git ls-files --stage socrates/dialectics
# Expected: 160000 <SHA> 0	socrates/dialectics

# Verify no old paths remain
git ls-files ".claude/skills/socrates" | wc -l
# Expected: 0

# Verify git status is clean for .claude/
git status --short | grep '.claude/skills/socrates' | wc -l
# Expected: 0
```

**Step 6: Commit the index fix**

```bash
git add .gitmodules  # Ensure .gitmodules is staged (it may already be if it was modified in Phase 6)
git commit -m "fix(10): register submodule gitlink at socrates/dialectics and remove old .claude/ paths"
```

Note: The `.gitmodules` file was already corrected in Phase 6 (references `socrates/dialectics`) but may need re-staging since the submodule name changed in the index.

**Anti-patterns to avoid:**
- Do NOT use `git rm` without `--cached` — files don't exist on disk, it will error
- Do NOT use `git submodule add` — `.gitmodules` already has the entry, `submodule add` will fail or duplicate
- Do NOT skip removing the gitlink separately before the recursive `git rm` — gitlinks (mode 160000) behave differently in recursive removal
- Do NOT hardcode the SHA — always read it from `git ls-files --stage` immediately before the `update-index` command
  </action>
  <verify>
    <automated>git ls-files --stage socrates/dialectics | grep -q "^160000" && echo "GITLINK OK" && test "$(git ls-files '.claude/skills/socrates' | wc -l)" -eq 0 && echo "OLD PATHS REMOVED" && test "$(git status --short | grep '.claude/skills/socrates' | wc -l)" -eq 0 && echo "GIT STATUS CLEAN" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - git ls-files --stage socrates/dialectics returns mode 160000 with correct SHA
    - git ls-files .claude/skills/socrates returns 0 entries
    - git status shows no unstaged deletes under .claude/skills/socrates/
    - Changes committed in a single atomic commit
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 6 VERIFICATION.md and update ROADMAP.md progress</name>
  <files>.planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md, .planning/ROADMAP.md</files>
  <action>
This task creates the missing VERIFICATION.md for Phase 6 (closing the verification gap for 5 requirements) and updates ROADMAP.md to reflect Phase 6 completion. No tests are re-run — this is evidence synthesis from existing documents.

**Step 1: Create 06-VERIFICATION.md**

Create `.planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md` following the exact format of `07-VERIFICATION.md` (Phase 7's verified example). Use the following structure:

**Frontmatter:**
```yaml
---
phase: 06-plugin-scaffold-and-path-migration
verified: 2026-03-01T<current_time>Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---
```

**Phase Goal:** (copy from ROADMAP Phase 6 goal): "Users who install the plugin via `--plugin-dir` can invoke `/socrates` and have all protocol file reads resolve correctly — manifest exists, directory structure matches plugin conventions, and every hardcoded path is replaced with `$CLAUDE_PLUGIN_ROOT`"

**Observable Truths table** — map each to UAT evidence:

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | plugin.json exists at socrates/.claude-plugin/plugin.json with name, version, description, author, homepage, repository, and license | VERIFIED | 06-01-SUMMARY: plugin.json created with name=socrates-skill, version=0.1.0; UAT test 1: PASS |
| 2 | Plugin version follows semver (0.1.0) and enables update detection | VERIFIED | 06-01-SUMMARY: version=0.1.0 in plugin.json; UAT test 1: PASS |
| 3 | User installs via --plugin-dir and /socrates preflight reads $CLAUDE_PLUGIN_ROOT/socrates/protocols/dialectics.opt.cue without error | VERIFIED | 06-02-SUMMARY: end-to-end verified via --plugin-dir; UAT test 3: PASS |
| 4 | All protocol file Read references in SKILL.md use $CLAUDE_PLUGIN_ROOT/socrates/protocols/ prefix — zero old .claude/skills/socrates/ paths remain | VERIFIED | 06-02-SUMMARY: 24 old refs removed, 23 new refs added (1 error message ref removed entirely); UAT test 4: grep returns 0 old paths |
| 5 | SKILL.md lives at socrates/skills/socrates/SKILL.md and slash command registers correctly under plugin namespace | VERIFIED | 06-01-SUMMARY: SKILL.md moved to autodiscovery location; UAT test 2: /socrates available via --plugin-dir |

**Required Artifacts table:**

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| socrates/.claude-plugin/plugin.json | Plugin manifest with identity and semver | VERIFIED | Created in 06-01 (commit fc425ca); 7 required fields present |
| socrates/skills/socrates/SKILL.md | Skill entrypoint at autodiscovery location | VERIFIED | Moved in 06-01 (commit fc425ca); all frontmatter preserved |
| .gitmodules | Corrected submodule path reference | VERIFIED | Updated in 06-01 (commit fc425ca); references socrates/dialectics |

**Key Link Verification table:**

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| plugin.json manifest | socrates/skills/socrates/SKILL.md | Plugin autodiscovery convention | WIRED | plugin.json at socrates/.claude-plugin/ + SKILL.md at socrates/skills/socrates/ = standard plugin layout |
| SKILL.md path references | socrates/protocols/**/*.opt.cue | $CLAUDE_PLUGIN_ROOT expansion | WIRED | All 23 path references use $CLAUDE_PLUGIN_ROOT/socrates/ prefix; empirically verified in 06-02 UAT |
| .gitmodules | socrates/dialectics | Submodule path declaration | WIRED | .gitmodules references socrates/dialectics; gitlink registration fixed in Phase 10 Task 1 |

**Requirements Coverage table:**

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PLUG-03 | 06-01-PLAN | Plugin manifest includes name, version, description, author, homepage, repository, license | SATISFIED | 06-01-SUMMARY requirements-completed: [PLUG-03]; UAT test 1 PASS; commit fc425ca |
| PLUG-04 | 06-01-PLAN | Plugin version follows semver, enables update detection | SATISFIED | 06-01-SUMMARY requirements-completed: [PLUG-04]; version=0.1.0; UAT test 1 PASS |
| PATH-01 | 06-02-PLAN | /socrates resolves via $CLAUDE_PLUGIN_ROOT after plugin install | SATISFIED | 06-02-SUMMARY requirements-completed: [PATH-01]; UAT test 3 PASS (preflight + execution) |
| PATH-02 | 06-02-PLAN | Preflight reads $CLAUDE_PLUGIN_ROOT path | SATISFIED | 06-02-SUMMARY requirements-completed: [PATH-02]; UAT tests 3+5 PASS |
| PATH-03 | 06-02-PLAN | All path refs use $CLAUDE_PLUGIN_ROOT prefix | SATISFIED | 06-02-SUMMARY requirements-completed: [PATH-03]; UAT test 4 PASS (grep returns 0) |

**Anti-Patterns Found:** None. No TODO/FIXME/HACK/placeholder in Phase 6 artifacts.

**Human Verification Required:** None for this verification pass. The empirical human verification was already performed during Phase 6 execution and captured in 06-UAT.md (5/5 tests passed).

**Gaps Summary:** Note that the local machine's `.git/modules` directory still names the submodule as `.claude/skills/socrates/dialectics` and the `.git` file inside `socrates/dialectics/` has an incorrect relative path. These are local-machine-only issues that do not affect committed state or fresh clones. Document this as a known local state artifact.

**Commit Evidence:**

| Commit | Message | Files |
|--------|---------|-------|
| fc425ca | feat(06-01): create plugin manifest and restructure directory | 3 |
| 418a330 | feat(06-02): migrate all 24 path references and update preflight message | 1 |

**Step 2: Update ROADMAP.md**

Make these 4 targeted edits to `.planning/ROADMAP.md`:

1. **Phase 6 milestone bullet** — change `- [ ]` to `- [x]` and append ` (completed 2026-03-01)`:
   - FROM: `- [ ] **Phase 6: Plugin Scaffold and Path Migration** - Plugin manifest created, directory restructured, all SKILL.md path references migrated to \`$CLAUDE_PLUGIN_ROOT\``
   - TO: `- [x] **Phase 6: Plugin Scaffold and Path Migration** - Plugin manifest created, directory restructured, all SKILL.md path references migrated to \`$CLAUDE_PLUGIN_ROOT\` (completed 2026-03-01)`

2. **Phase 6 Plan 01 checkbox** in Phase Details:
   - FROM: `- [ ] 06-01-PLAN.md — Create plugin manifest, restructure directory, test --plugin-dir path resolution`
   - TO: `- [x] 06-01-PLAN.md — Create plugin manifest, restructure directory, test --plugin-dir path resolution`

3. **Phase 6 Plan 02 checkbox** in Phase Details:
   - FROM: `- [ ] 06-02-PLAN.md — Migrate all 24 path references in SKILL.md and verify end-to-end`
   - TO: `- [x] 06-02-PLAN.md — Migrate all 24 path references in SKILL.md and verify end-to-end`

4. **Progress table row** for Phase 6:
   - FROM: `| 6. Plugin Scaffold and Path Migration | v1.1 | 2/2 | Unverified | - |`
   - TO: `| 6. Plugin Scaffold and Path Migration | v1.1 | 2/2 | Complete | 2026-03-01 |`

5. **Phase 10 plan list** — update from TBD:
   - FROM: `**Plans**: TBD`
   - TO: `**Plans**: 1 plan`
   - Add: `Plans:\n- [ ] 10-01-PLAN.md — Fix git index, create Phase 6 VERIFICATION.md, update ROADMAP progress`

**Step 3: Verify changes after editing**

Run `grep -n "06-0" .planning/ROADMAP.md` to confirm all three Phase 6 locations show `[x]` checkboxes. Run `grep -n "Unverified" .planning/ROADMAP.md` to confirm zero occurrences remain (Phase 6 was the only Unverified phase).

**Anti-patterns to avoid:**
- Do NOT re-run UAT tests — all 5 passed, evidence is captured in 06-UAT.md
- Do NOT copy Observable Truths verbatim from ROADMAP Success Criteria without mapping to actual UAT evidence — some counts differ (e.g., "~18 protocol file Read references" in criteria vs 23 actual references confirmed in UAT)
  </action>
  <verify>
    <automated>test -f .planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md && grep -q "status: passed" .planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md && grep -q "PLUG-03" .planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md && grep -q "PATH-03" .planning/phases/06-plugin-scaffold-and-path-migration/06-VERIFICATION.md && echo "VERIFICATION.md OK" && grep -q "\[x\] 06-01-PLAN" .planning/ROADMAP.md && grep -q "\[x\] 06-02-PLAN" .planning/ROADMAP.md && grep -q "Complete | 2026-03-01" .planning/ROADMAP.md && echo "ROADMAP OK" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - 06-VERIFICATION.md exists with status: passed, score: 5/5, all 5 requirements listed as SATISFIED
    - ROADMAP.md Phase 6 milestone bullet shows [x] with completed date
    - ROADMAP.md Phase 6 plan checkboxes both show [x]
    - ROADMAP.md progress table shows Phase 6 as "Complete | 2026-03-01"
    - Changes committed
  </done>
</task>

</tasks>

<verification>
- [ ] `git ls-files --stage socrates/dialectics` returns mode 160000 gitlink entry
- [ ] `git ls-files .claude/skills/socrates | wc -l` returns 0
- [ ] `git status` shows no unstaged deletes under `.claude/skills/socrates/`
- [ ] `06-VERIFICATION.md` exists with `status: passed` and all 5 requirements (PLUG-03, PLUG-04, PATH-01, PATH-02, PATH-03) listed as SATISFIED
- [ ] `ROADMAP.md` Phase 6 shows `[x]` for milestone bullet, both plan checkboxes, and "Complete" in progress table
- [ ] No occurrences of "Unverified" remain in ROADMAP.md for Phase 6
</verification>

<success_criteria>
Repository state is clean: submodule gitlink registered at the correct path, all old `.claude/skills/socrates/` paths removed from HEAD, Phase 6 requirements formally verified via VERIFICATION.md, and ROADMAP.md accurately reflects Phase 6 completion. Downstream phases (8, 9) can proceed against a clean baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/10-repository-cleanup-and-phase6-verification/10-01-SUMMARY.md`

The SUMMARY must include:
- The exact git index operations performed (SHA used, paths removed)
- Confirmation that 06-VERIFICATION.md synthesizes existing evidence (no re-run of UAT)
- The 4 ROADMAP.md edits made
- Any local machine state observations (broken .git/modules naming, etc.)
</output>
